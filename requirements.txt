# Core scientific stack — pinned to versions tested with the framework.
# Tested on Python 3.12.11 (macOS 14.x, Linux). Python 3.11+ should work.
numpy==1.26.4
pandas==2.3.3
scipy==1.16.1
matplotlib==3.10.5

# Optional: jupyter only if you want to explore the analyses interactively.
# Uncomment if needed.
# jupyter==1.0.0
# ipython==8.30.0
